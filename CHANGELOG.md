# 更新日志

这里记录每个版本里用户能感觉到的东西：新增了什么、修了什么、有什么坑要避开。
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.2.0] — 2026-09-18

这一版是围着**真机**做的：拿一台 Voron 2.4（Klipper / Moonraker）把画面、打印信息、
操作项摸清楚，再按需要补齐。

### 修复

- **有些机器的画面永远出不来**：Moonraker 给的摄像头地址是相对路径
  （`/webcam/?action=snapshot`），它挂在**主机 80 端口**上，而程序把它拼在了 Moonraker
  端口上，于是 404。真机实测：80 端口返回 200 和约 28 KB 的 JPEG，7125 端口返回 404。
  这就是「遥测正常、端口也通，却一直没有画面」的原因。
- **健康的连接被误判成「遥测卡死」**：老判据是「连接计数 90 秒没涨就算卡死」，
  而健康的长连接本来就不会让计数上涨 —— 于是画面和进度都正常却一直提示卡死，
  真卡死时反而要等满 90 秒。现在改成主动探活（发一次 `pushall`，看设备有没有回应）：
  在线且 45 秒内有报文就绝不重建，真卡死最多 53 秒就能确认。
- **两条自己造出来的假警报**：断料开关的 `filament_detected: true` 表示**有料**
  （第一版写反了，正常打印时显示「无料」）；走料检测给的是瞬时值，挤出间隙读到 false
  很正常（第一版翻译成「可能堵料」）。
- 普通 Moonraker **没有** `camera.start_monitor`，程序原来每 5 秒试一次并打一条告警；
  现在确认设备不认识它就永久关掉（U1 仍然保活）。
- 免鉴权的 Moonraker 没填 API Key 时，状态行会显示「未配置 API Key」——那是完全正常的
  状态，不再报这句。

### 新增

- **多路画面**：一台机器可以有多路（Voron 常见「喷嘴 + 舱内」）。能列出、能切换
  （桌面右键菜单、网页下拉），选中的那一路记进配置；取不到帧的那一路会标明原因，
  而不是从列表里消失。
- **详细读数**：按设备实际配置发现对象。Voron 上能列出 18 项 —— 已打印与累计时长、
  耗材用量、文件位置、喷嘴与热床的加热功率、压力提前、实时位置、归零状态、
  速度与流量倍率、工具头板温度、5 个风扇、断料与走料检测、主机负载、MCU 固件。
  桌面版并 3 条进信息行、完整清单进 tooltip；网页卡片加默认收起的「详情」。
- **剩余时间**：Klipper 没有原生字段，用「已打印时长 ÷ 进度」外推；进度太小或没有时长时
  不猜，仍然显示 `--`。
- **网页端补齐桌面版的能力**：刷新视频流、选摄像头、轮巡、单画面与全屏、抓拍保存、
  全部连接/断开、画面顺序（上移 / 下移 / 移到最前 / 移到最后）。
- **灯光开关（第三方设备）**：Klipper 机器上「舱灯」没有统一做法 —— 可能是宏、输出引脚、
  也可能是某个风扇。所以做成可配置：在添加/编辑设备里填「开灯 G-code」「关灯 G-code」，
  例如 `LIGHT_ON` 或 `SET_PIN PIN=caselight VALUE=1`。留空表示这台设备没有可控灯光，
  界面也就不会出现一个按下去没反应的灯按钮。

### 已知限制

- **你的 Voron 需要自己填灯光命令**：程序在它的 60 个对象里没有找到任何可控制的灯
  （没有 `led` / `neopixel` / `output_pin`，也没有灯光宏）。知道灯接在哪之后填一下即可。
- 遥测探活的阈值（45 秒静默 + 8 秒等回应）是按常见情况估的，网络特别慢可以反馈。
- 网页端的轮巡 / 全屏 / 抓拍只做了代码层验证（没有浏览器自动化），安卓 WebView 同理；
  实际观感需要人工看一眼。
- 控制类功能（暂停 / 停止 / 灯光）这一版**没有在真机上实按过** —— 开发时机器正在打印，
  按约定只做只读验证。

## [1.1.0] — 2026-09-18

支持 Klipper / Moonraker 设备：Voron、RatRig/RatOS、刷了 Klipper 的 Creality /
Elegoo / Anycubic，以及出厂就是 Klipper 的 Snapmaker U1。

### 新增

- 添加设备时多了「设备族」选项，选 Klipper / Moonraker 就能监控这类机器。
  桌面版、网页和平板、命令行都能加。
- 凭据和端口跟着设备族走：拓竹填访问代码（默认端口 8883），Moonraker 填 API Key
  （内网通常留空，默认端口 7125，U1 常用 80），另外可以填摄像头地址。
- 命令行加第三方设备：

  ```bash
  python -m app.headless --add-printer "moonraker@Voron 192.168.1.90 abc123 7125"
  ```

- 进度、温度、层数、文件名、暂停/继续/停止都能用；灯和速度档位需要设备支持
  （Moonraker 只开放给 WebSocket，程序会自己处理）。

### 修复

- 编辑设备后保存，不再把设备族、端口、API Key、摄像头地址丢掉。
- 换了设备族会重建连接（原来只重连，等于用旧方式去连新设备）。
- 必填凭据没填时会当场报错，而不是加进去之后一直连不上。

### 已知限制

- 第三方设备不会被自动搜索发现（Moonraker 的 mDNS 默认是关的），手动填 IP。
- Moonraker 没有剩余时间字段，所以这一项不显示。宁可不显示，也不猜一个错的。
- 画面是定时抓快照，帧率比拓竹的 RTSPS 低；U1 需要手动填摄像头地址。
- 这一版只在本地假服务器上验证过，没有在真实的 Voron / U1 上跑过。
  第一次连真机建议先用桌面版的「测试连接」。

## [1.0.6] — 2026-09-18

安卓版能看到 X1 / X1C / X2D / H2 / P2S 的画面了。这些机型只提供 RTSPS(322) 通道，
而安卓版为了兼容新设备不打包 OpenCV，所以以前一直只有一句「连接中」。

### 修复

- 根因是没连对端口：没有 OpenCV 时，程序会放弃 322 去试 6000，
  而这些机型在 6000 上必然失败，等于从头到尾没连过正确的通道。
- 现在服务端只负责把 H.264 码流取下来，解码交给手机浏览器自带的解码器，
  安卓端一个原生库都不用装。
- 画面出不来时，会把原因和下一步直接写在画面区域，不再只显示「连接中」。

### 新增

- 三个排障脚本：`tools/rtsp_channel_check.py`（只看 322 通道）、
  `tools/rtsp_h264_check.py`、`tools/h264_stream_check.py`（端到端验证取流到前端这一整条）。

### 已知限制

- 安卓上解码依赖系统 WebView（94 以上，一般会自动更新）。太旧的设备仍然看不到画面，
  但页面上会写明原因，遥测不受影响。
- 「画面在安卓设备上真的画出来」这一步没有真机验证过（手边没有安卓设备）。
  已验证的是取流、传输、前端拿到可解码的数据（真机取样 65 帧全部解码成功）。
  如果安卓上还是黑屏，把「设置 → 通道诊断」的结果发出来。
- 首帧要等一个关键帧，所以刚点开时比 6000 端口的 JPEG 稍慢一点。
- APK 用固定发布证书签名：要覆盖安装，得与 1.0.4 及更早的包签名一致，
  更早的 debug 签名包需要先卸载。
- Windows exe 没有代码签名，可能被杀毒软件误报。
- 网页和控制走的是局域网 HTTP，网页令牌不是身份认证，别把端口暴露到公网。

## [1.0.5] — 2026-09-18

两件事：把「暂停/停止点了没反应」说清楚，以及让配置能在各版本之间搬。

### 新增

- 导出配置时可以用口令保护。带口令的文件在 Windows / Linux / Docker / 安卓之间通用，
  换机器也不用重填访问代码；导入时如果有凭据解不开，会点名是哪几台设备。
  入口：桌面版菜单、网页和平板的「设置 → 配置备份」、命令行
  `--export-config` / `--import-config` / `--config-passphrase`。
- 网页和平板补齐了桌面版才有的功能：配置备份、通道诊断、重点画面（2×2）。
- 控制被固件挡住时，提示写得更有用：必须先开局域网模式、再开开发者模式，
  顺序不能反；这种情况下只有灯能控制，也可以改用官方的农场管家。

### 修复

- 1.0.1 的文案把「局域网模式」和「开发者模式」写成二选一，实际只开局域网模式没有用：
  画面和遥测都正常，暂停/停止仍然被固件忽略。现在三个界面都按正确顺序说明。
- 桌面上被挡住的暂停/停止按钮以前点不动也看不到原因（Qt 的禁用控件收不到鼠标事件），
  现在按钮可以点，点一下弹出完整步骤。
- 控制按钮的样式在第一次刷新后被冲掉，现在是在原样式上叠加。
- `run.bat` 能自愈坏掉的 `.venv`（原来只检查文件在不在，基础 Python 被删后窗口一闪就没）。
- Linux / Docker / NAS 上访问代码不再明文保存：可以用配置目录里的密钥文件，
  或用 `BAMBU_MONITOR_SECRET` 口令派生。安卓仍是明文（装不了加密库）。
- 纯内网环境下搜不到设备：网卡枚举不再依赖能否访问外网，也能用
  `BAMBU_MONITOR_SUBNETS` 手工指定网段。
- Docker / NAS 上大图不做缩放、白白多花手机流量，现在用 OpenCV 兜底缩放。
- 点「导出配置」直接报错（`QStandardPaths` 从错误的模块导入）。
- 导出再导入会丢一半设置（超时、时间戳、自动连接、网页开关、重点画面标记）。
- 自动搜索结果里同一台打印机出现两行。
- 状态行把中文提示截成半句话。
- 关掉「通道诊断」或「添加打印机」对话框时，程序有可能直接消失（Qt 的 fail-fast，
  没有报错也没有日志）。
- 开启网页监控时每次都会弹出「网页信息」窗口。

### 已知限制

- 这一版的安卓版看不到 RTSPS 机型的画面（X1 / X1C / X2D / H2 / P2S），1.0.6 已修。
- 页面与控制走 HTTP，别暴露到公网，远程访问用 VPN 或反向代理（见 `SECURITY.md`）。
- Android 包用固定发布证书签名，换签名需要卸载重装。

## [1.0.4] — 2026-09-14

安卓专项：修闪退、修入口模块漏打包、修图标空白，并改用固定签名。

### 修复

- 新 ARM 设备（16 KB 内存页，如 Pixel 8 及以后的机型）上启动就闪退。
  原因是 Chaquopy 仓库里的 OpenCV / numpy / cryptography 预编译库按 4 KB 对齐编译，
  16 KB 页的设备会拒绝加载。现在安卓端不再安装这三个包（代码里本来就是可选的），
  APK 从 33 MB 降到 19 MB。
- 启动报 `ModuleNotFoundError: No module named 'bootstrap'`：安卓入口模块与 Chaquopy
  自带的同名文件冲突被丢掉，已改名 `device_server.py`，并在发布流程里加校验。
- 桌面图标空白（缺自适应图标定义，Android 8 以上会渲染成空白方块）。

### 变更

- 改用固定签名的发布证书。以前每次构建的签名都不一样，用户无法覆盖安装
  （系统报「应用未安装」）。本版签名与更早版本不同，需要先卸载旧版（应用内配置会一起清掉）。

## [1.0.3] — 2026-09-13

安卓版后台保活，以及 ChromeOS / 桌面模式兼容。其它平台的产物与 1.0.2 相同。

### 新增

- 安卓前台服务：常驻通知、息屏后 CPU 继续跑、Wi-Fi 不休眠、被系统回收后自动重建。
  在平板上当监控屏长期开着，以前切后台或息屏会被系统冻结甚至杀掉。
- 首次启动会问一次是否加入电池白名单，并跳到系统设置页（国产 ROM 不加白名单仍可能被杀）。
- ChromeOS 与桌面模式：不强制要求触摸屏，窗口可以自由缩放，
  沉浸式全屏只在真的有触摸屏的设备上启用。

### 变更

- 通知栏常驻一条「后台运行中（端口 8080）」，点它回到界面。
- 服务失联对话框多了「去设置」；「退出」改成「关闭界面」，并说明后台仍在运行。

## [1.0.2] — 2026-09-12

修两个安卓上的问题：遥测断开后不会自己恢复，以及看不到访问令牌，导致没法在电脑上打开。

### 修复

- 遥测断连后不再自愈。paho 自己会重连，但它也会卡死：息屏、切网之后底层连接坏掉时，
  重连一直失败却不报错；连接流程里的 TLS 探测曾经没有异常保护，后台线程一旦抛出异常
  就静默退出，遥测永久停住，只能手动重连。现在连接流程兜住所有异常并把原因写进状态，
  另外加了个看门狗：90 秒没有成功连接过就整条重建。
- 网页里看不到访问令牌。令牌只在打开页面的那一刻出现在地址里，随后被前端抹掉，
  而安卓版没有终端可以看启动日志，结果就是平板本机能用、却没法在电脑上打开。
  现在网页「设置」底部有「在其它设备上打开」，逐条列出带令牌的地址，可以一键复制。

### 顺带修掉的两个问题

- `/api/info` 读了一个不存在的属性，实际返回 500。
- 接口返回的端口在系统分配临时端口时是 0，用户拿到的是 `http://192.168.x.x:0/` 这种不可用的地址。

## [1.0.1] — 2026-09-12

只改提示文案，没有功能变更。单独发一版是因为旧文案会把人引向错误的排查方向。

### 修复

- 网页状态条的提示原本文案是「控制不可用」，但它的触发条件只是「固件要求命令签名」。
  两个问题：一是把范围说大了——签名只影响暂停/继续/停止/调速，**灯控不受影响**，
  用户看到「控制不可用」却发现灯能开关，反而更糊涂；二是只写了解法之一。
  现在按具体被挡的命令逐条说明，并给出三种解法：

  | 方式 | 位置 |
  | --- | --- |
  | 局域网模式 | 设置 → 网络 → 局域网模式 / LAN Mode Only |
  | 开发者模式 | 设置 → 通用 → 开发者模式 / Developer Mode |
  | 农场管家 | 改用官方 Farm Manager 接管设备 |

- 同时写清楚了：只在切片软件里登录拓竹账号没有用（那只影响云端）。

### 文档

- `docs/FIELD_NOTES.md` 补上三条放行方式、账号登录无效的实测结论，以及
  「签名只覆盖 print 段、灯控走 system 段」这个事实。
- README 常见问题新增「能监控但暂停/停止点了没反应」。

## [1.0.0] — 2026-09-12

首个公开版本。四种交付形态共用同一套 Python 内核，网页是各平台统一的操作界面。

### 新增

- 遥测：通过打印机的局域网 MQTT 接口读取进度、剩余时间、喷嘴/热床/腔体温度、
  AMS 各料盘余量与类型、层数，以及 HMS 错误码的中文解释。
- 画面：6000 端口鉴权 JPEG 流，以及 X1 / X2D 这类机型的 RTSPS(322) 通道
  （需要 OpenCV），按机型和实际连通性自动选择。
- 自动搜索：SSDP 组播加上单播补扫和旧版 2021 端口广播。
- 设备管理：搜索、手动添加、改名、改访问代码、删除、重连，多台并排监控。
- 控制：暂停、继续、停止、打印速度、腔体灯。
- 网页端：一条连接同时推送所有画面和状态（绕开浏览器的同域 6 连接限制），
  可以「添加到主屏幕」当 PWA 用。
- 机型覆盖：X1 / X1C / X1E / P1P / P1S / A1 / A1 mini / A2L / P2S / H2D /
  H2D Pro / H2C / X2D，按序列号前缀与固件代号双重识别。
- 内置模拟器：伪造完整的打印机（TLS、JPEG 帧、发现协议），开发和测试不需要真机。

### 四种交付形态

| 形态 | 产物 | 目标机需要什么 |
| --- | --- | --- |
| Windows | 单文件 exe（自带 Python 运行时） | 什么都不用装 |
| Linux | 单文件（无界面服务版 / 带界面版） | 什么都不用装 |
| Docker | 单文件镜像 tar.gz（`docker load` 即用） | 只需 Docker |
| 安卓 | APK（arm64，内置 Python + OpenCV；1.0.4 起不再内置 OpenCV） | Android 7.0+ |

### 修复

开发过程中真实踩到、且只在特定平台暴露的问题：

- 安卓首次打开报 `unauthorized`：没有配置文件时每次读配置都会生成一个新令牌，
  宿主和服务各拿了一个。现在令牌只有一处来源，先落盘再返回。
- 安卓自动搜索里同一台设备出现两次：去重键只有「序列号或 IP」，
  同一台机器被两条通道发现时会登记成两条。改为序列号与 IP 双重索引。
- 安卓添加设备报「添加失败当前系统没有 DPAPI」，其实设备已经加上了：
  没有 DPAPI 时凭据退回明文保存是正常降级，却被当成了失败。
  现在区分「写盘失败」和「只是提示」。
- 安卓上偶发的「线程无法回收」异常：自定义属性名与标准库撞名，已改名。
- 开放机型（A2L）的灯控被误挡：签名门禁原来一刀切，实际只挡暂停/停止这类命令。
- Docker 容器空配置时进程直接退出，被重启策略反复拉起；现在会正常起服务。

### 已知限制

- 安卓 APK 没有在真机上跑过完整回归（构建机既没有安卓设备，也跑不动模拟器）。
- 固件要求命令签名时，暂停/继续/停止/调速会被设备忽略，界面会说明原因。
- Linux 和安卓没有 DPAPI，访问代码是明文保存，见 `SECURITY.md`（1.0.5 起 Linux 侧已改善）。

[1.1.0]: https://github.com/asahiba/bambu-monitor/releases/tag/v1.1.0
[1.0.6]: https://github.com/asahiba/bambu-monitor/releases/tag/v1.0.6
[1.0.5]: https://github.com/asahiba/bambu-monitor/releases/tag/v1.0.5
[1.0.4]: https://github.com/asahiba/bambu-monitor/releases/tag/v1.0.4
[1.0.3]: https://github.com/asahiba/bambu-monitor/releases/tag/v1.0.3
[1.0.2]: https://github.com/asahiba/bambu-monitor/releases/tag/v1.0.2
[1.0.1]: https://github.com/asahiba/bambu-monitor/releases/tag/v1.0.1
[1.0.0]: https://github.com/asahiba/bambu-monitor/releases/tag/v1.0.0
