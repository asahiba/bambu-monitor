# 换行符约定。
#
# 为什么需要这个文件：PowerShell 5.1 **不把只有 LF 的换行当作 here-string
# （@" … "@）的结束符**。若 Git 在 checkout 时把 .ps1 转成 LF，脚本会报
# 「The string is missing the terminator: "@」而指不到真正原因。
# 所以 .ps1/.bat 固定按 CRLF 检出。
# 注意：BOM 不在这里管 —— Git 的 text 归一化只碰换行、不碰 BOM，
# BOM 由 tests/test_contracts.py 的两个契约测试守住。
#
# 规则是「后面的覆盖前面的」，所以要放得比下面的通用 text 规则更靠后。

# 源码/文档一律 LF（仓库内既有惯例，也让 diff 稳定）
.gitattributes text eol=lf
.gitignore text eol=lf
*.py       text eol=lf
*.sh       text eol=lf
*.md       text eol=lf
*.toml     text eol=lf
*.yml      text eol=lf
*.yaml     text eol=lf
*.json     text eol=lf
*.gradle   text eol=lf
*.java     text eol=lf
*.xml      text eol=lf
*.spec     text eol=lf
Dockerfile* text eol=lf

# Windows 脚本必须 CRLF（PS 5.1 的 here-string 依赖它）
*.ps1      text eol=crlf
*.bat      text eol=crlf
*.cmd      text eol=crlf

# 二进制/产物：绝不做任何转换
*.png      binary
*.jpg      binary
*.jpeg     binary
*.ico      binary
*.gz       binary -text -diff
*.whl      binary
*.apk      binary
*.jar      binary
*.exe      binary
*.so       binary
*.pem      binary -text -diff
*.cer      binary
*.icns     binary
