# 开发/测试依赖（运行软件本身只需要 requirements.txt）
#
# 安装：
#   .venv\Scripts\python.exe -m pip install -r requirements-dev.txt
-r requirements.txt

# 测试框架
pytest>=8.0

# 静态检查（未使用导入 / 未定义名字 / import 排序）
ruff>=0.6

# 网页端内嵌 JS 的语法解析。
#
# 为什么需要：`app/web/page.py` 里的前端 JS 没有任何编译期检查，
# 写坏一个括号只会让页面白屏，而 Python 侧测试照样全绿。
# 装上它之后 `tests/test_contracts.py` 会真正解析一遍内嵌脚本
# （没装则跳过那条断言，不会让测试失败 —— 所以它是可选依赖）。
esprima>=4.0
