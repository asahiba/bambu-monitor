# 桌面版依赖（含 PySide6）
#
# ⚠️ 中文 Windows（cp936）上 pip 会用本地编码读本文件，中文注释会让
# `pip install -r requirements.txt` 报 UnicodeDecodeError。
# 先 `set PYTHONUTF8=1`（PowerShell：$env:PYTHONUTF8="1"）即可；
# 仓库自带的 run.bat / test.bat / build_*.bat 已内置这一行。
PySide6-Essentials>=6.5
paho-mqtt>=2.0
# 可选依赖：X1 / P2S / H2 / X2D 的 RTSPS(322) 视频通道（更流畅）
opencv-python>=4.8
# 可选依赖：仅内置模拟器（--sim / --core-test）需要，用于生成自签证书
cryptography>=41
